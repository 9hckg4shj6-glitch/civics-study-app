# 復習機能の改善 — 設計と作業計画

最終更新: 2026-09-07  
基準コミット: `4524528`（作業記録に2010年分のコミットを書き入れる）  
対象: ホーム画面の「復習」、復習画面、解説直後の表示  
関連文書: `NEXT_WORK.md`（作業管理の正本）、`README.md`（開発・公開方法）

## 1. この文書の目的

復習機能を忘却曲線の原理どおりに働かせ、「次にいつ復習すればよいか」を利用者が常に見えるようにするための設計と作業計画をまとめる。

教材追加の作業手順は `NEXT_WORK.md` を正本とし、この文書は復習機能に限った設計の正本とする。

## 2. 解決したい課題

利用者から次のフィードバックが得られている。

- 問題を解いたあと、その問題を何日後に復習すればよいのかがわからない。
- 復習を開こうとしても、間違えた問題がそのまま並ぶだけで、復習した実感がない。

原因は次の2点である。

1. **予定が画面に出ていない。** FSRS（`ts-fsrs`）で次回復習日時は計算していたが、その結果を利用者に見せる場所がどこにもなかった。解説の下に出るのは「約N日後」という目安の一文だけで、問題ごとの予定は追えなかった。
2. **予定の粒度が日付だった。** 復習の対象は `r.due <= 今日の日付` で判定していた。FSRSが「20分後」「4時間後」と出しても日付に丸められ、当日中の短い間隔がすべて消えていた。学習直後の反復が効かないため、忘却曲線に沿わない。

## 3. 目指す体験

1. 問題を解いて手ごたえを選ぶと、その場で「次の復習：約10分後（9/7 19:20）」と表示される。
2. ホームの復習カードに、いま復習すべき件数と、0件なら次の予定時刻が出る。
3. 復習画面を開くと「本日復習すべき問題」に時刻を迎えた問題が並び、そこから復習演習に入れる。
4. その下の「これからの復習予定」で、まだ時刻が来ていない問題を大分野・小分野で絞って一覧できる。
5. 予定時刻は放置していても更新される。時刻を過ぎた問題は自動で「本日復習すべき問題」へ移る。

## 4. 現状 — 実装は作業ツリーに入っている

この改善の中心部分は、**すでに未コミットの変更として作業ツリーに存在する**。ゼロからの実装ではなく、残りの穴を埋める作業になる。

変更されているファイルは次のとおり。

| ファイル | 変更の要点 |
| --- | --- |
| `index.html` | 時刻単位の予定計算、復習画面の予定一覧、手ごたえの選び直し |
| `src/fsrs.ts` | 保存のカードごと直列化、冪等な保存、保存失敗の通知 |
| `src/main.ts` | `STUDY_CORE.ready` の公開、起動完了の通知 |
| `src/global.d.ts` | `scheduleReview` の引数追加、`ready` の型 |
| `src/legacy-bridge.ts` | 予定の差分判定に `fsrs` 本体を含める |
| `src/modern.css` | `.reviewQuestion` / `.reviewSchedule` の追加 |
| `tests/review-schedule.test.ts` | 新規。時刻判定と冪等保存の検証 |
| `tests/app-smoke.test.ts` | jsdom 用に実物のFSRS計算を注入 |

検証状態は `tsc --noEmit` 通過、`vitest run` で104件すべて成功。

### 4.1 実装済みの内容

**予定時刻の計算（`index.html`）**

- `reviewAt(r)` — `r.fsrs.due`（ISO時刻）を優先し、無ければ旧来の `r.due`（`YYYY-MM-DD`）を現地時刻の午前0時として解釈する。どちらも無ければ `null`。
- `reviewText(r, now)` — 残り時間を文言にする。1時間未満は「分」、24時間未満は「時間」、それ以上は「日」。過ぎていれば「予定から約N経過」、前後1分以内は「復習の時間です」。学習済みで予定が無ければ「予定未設定」、未学習なら「未学習」。
- `reviewOrder(a, b)` — 予定時刻の早い順。予定なしは末尾。
- `dueItems()` — 予定時刻が現在時刻以下の問題。日付比較から時刻比較へ変えた核心部分。
- `nextDueInfo()` — まだ時刻が来ていない問題のうち最も早いもの。
- `scheduleBadge(q)` — 「次の復習：…」の一行。`data-review-id` を持ち、時計更新でまとめて書き換えられる。

**画面（`index.html` / `src/modern.css`）**

- 復習画面に `#reviewDueList`（本日復習すべき問題）、`#reviewScheduleList`（これからの復習予定）、`#reviewDomain` / `#reviewField`（絞り込み）、`#reviewPractice`（問題演習へ）を追加。
- 一覧の各行は問題ボタンで、押すとその1問だけの復習演習に入る。
- 解説の下と問題一覧の各項目にも `scheduleBadge()` を出す。
- ホームの見出しを「今日の復習」から「本日復習すべき問題」へ、復習画面の見出しを「復習する時期がひと目でわかる」へ変更。

**手ごたえの選び直し（`index.html` の `gradeCard`）**

回答ごとに `reviewBase`（採点前の記録の複製）、`reviewEventId`、`reviewTime` を持たせる。手ごたえを押し直すたび、毎回この採点前の状態から計算し直す。同じ回答は同じ `reviewEventId` を使うため、反復回数が二重に増えず、保存も1件に収まる。

**保存の健全性（`src/fsrs.ts`）**

- カードIDごとに保存の約束を直列化し、押し直しの競合で古い結果が後勝ちしないようにした。
- `reviewEvents.add` を `put` に変え、同じ `eventId` の保存を上書きにした。
- 保存に失敗したら `study:review-save-failed` を投げ、`retry` を渡す。

**起動前の保護（`index.html` / `src/main.ts`）**

モジュール読み込み前に演習を始めると、FSRSなしで予定が壊れた記録を作ってしまう。`STUDY_CORE.ready` を見て、未準備なら演習・フラッシュカードの開始と解答を止め、案内を出す。旧来のフォールバック計算（`ease` を手で増減させる簡易SRS）は、FSRSと違う予定を書き込んでしまうため削除した。

**時計の更新（`index.html` の `refreshReviewClock`）**

30秒ごと、ウィンドウ復帰時、可視状態への復帰時、保存完了時に予定表示を描き直す。

## 5. 残作業（2026-09-07 に対応済み）

A〜Dはすべて実装し、`npm run check` と実ブラウザで確認した。以下は何をなぜ直したかの記録として残す。

### A. 記述式・まとめて採点を新しい経路に載せる

**現状の問題。** 選択式1問ずつの経路だけが `gradeCard()` を通る。記述式の採点（`index.html` の約5689行）と、まとめて採点（約3685・3691行）は `scheduleSRS()` を直接呼んでいる。このため次が起きる。

- 冪等な `eventId` が付かず、採点し直すと反復回数が二重に増える。
- 採点をやり直せない。
- 「次の復習」バッジが出ない。

**方針。** 両方を `gradeCard(grade, entry)` 経由に統一する。`gradeCard` は採点前の状態から計算し直して文言を返すので、記述式では返り値をそのまま結果表示に使う。まとめて採点では各設問の `entry` に対して呼ぶ。

**注意。** 記述式は部分点から手ごたえ（1〜4）を決めている既存の換算をそのまま使い、換算結果を `gradeCard` に渡すだけにとどめる。採点基準そのものは変えない。

### B. 講義カードを復習の流れに戻す

**現状の問題。** `openDueReview()` から講義カード（用語カード）の経路が消え、`renderDue()` の件数も問題だけになった。`dueTermItems()` は `r.due <= 今日の日付` という旧ロジックのまま残っている。結果、**時期の来た講義カードにホームの復習カードから到達できない**。`dueReviewCards()` は暗記カード画面（約3944・4086行）からは今も使われているため、機能自体は生きたまま入口だけが失われている。

**方針。**

1. `dueTermItems()` を `reviewAt()` による時刻判定へ変える。問題と同じ土俵に乗せる。
2. `renderDue()` の件数を問題と講義カードの合計に戻す。文言は「今すぐ復習 N問・カードM枚」の形にする。
3. `openDueReview()` に講義カードの分岐を戻す。カードを含む日は、問題とカードを混ぜたデッキをフラッシュカードで復習する（改善前の挙動）。カードが無ければ従来どおり問題数の確認画面へ進む。
4. `nextDueInfo()` の走査対象に講義カードを含める。
5. 自分のデッキの `startPersonalDeck()`（約3915行）も日付比較が残っているので、同じく時刻判定へそろえる。

### C. 小分野の絞り込みを大分野に連動させる

**現状の問題。** `#reviewField` を全問題の小分野から作っているため、「政治」を選んだうえで公共にしかない小分野を選ぶと必ず0件になる。

**方針。** 大分野が選ばれているときは、その大分野に属する小分野だけを選択肢にする。大分野を変えたとき、選択中の小分野がその大分野に無ければ「すべて」へ戻す。選択肢は学習済みの問題があるものに限る（予定一覧に出ないものを並べても選べない）。

### D. 細かい不具合

1. **保存失敗の表示が重なる。** `study:review-save-failed` のたびに素の `div` を `body` へ足しているため、失敗するほど積み上がる。既存の1つを使い回す形にし、アプリの見た目（`toast` か `panel`）にそろえる。
2. **結果画面の案内が重複する。** `renderResult()` の `setTimeout` が呼ばれるたびに `<p>` を足す。既存の要素を探して書き換える形にする。
3. **30秒ごとに一覧を全部作り直している。** 数百件の `innerHTML` を再構築するため、スクロール位置が飛び、負荷も無駄になる。時計更新では `data-review-id` を持つ文言の書き換えだけを行い、一覧の並び替えが必要になったとき（時刻を過ぎた問題が出たとき）だけ作り直す。

## 6. 変更しないもの

`NEXT_WORK.md` の「8. 変更してはいけないもの」に加え、この作業では次を守る。

- 既存の問題ID、`progress` に入っている学習記録の項目名。
- IndexedDB名 `civics-study-v1`、`civics` で始まる localStorage キー、バックアップ識別子 `common-test-civics`。
- FSRSのパラメータと採点基準。この改善は「計算結果の見せ方」と「保存の正しさ」の話であり、間隔の計算そのものは変えない。
- 記述式の部分点と採点基準。

## 7. 検証手順

1. `npm run check`（ビルド、教材検証、`tsc --noEmit`、`vitest run`）を通す。
2. `tests/review-schedule.test.ts` に、記述式・まとめて採点でも1回答が1イベントに収まることの確認を足す。
3. 実ブラウザ（PC幅とスマートフォン幅390px）で次を確認する。
   - 選択式・記述式・まとめて採点のいずれでも、解答後に「次の復習」が出る。
   - 手ごたえを押し直しても反復回数が増えず、表示が押した値に追従する。
   - 時刻を迎えた問題が「本日復習すべき問題」へ移る（端末の時刻を進めて確認する）。
   - 時期の来た講義カードがホームの復習カードから復習できる。
   - 大分野を切り替えると小分野の選択肢が連動する。
4. 学習記録のバックアップを書き出し、リセットしてから復元し、予定が元どおりになることを確認する。

## 8. 作業記録

| 日付 | コミット | 実施内容 | 検証結果 | 公開 |
| --- | --- | --- | --- | --- |
| 2026-09-07 | 未コミット | 予定を日付単位から時刻単位へ変更。復習画面に「本日復習すべき問題」「これからの復習予定」を追加。手ごたえの選び直しを冪等化。起動前の演習開始を保護 | `tsc` 通過・104テスト成功 | 未 |
| 2026-09-07 | （このコミット） | 残作業A〜Dを実施。記述式とまとめて採点を `gradeCard()` に統一、講義カードを時刻判定へそろえて復習の入口を復活、小分野を大分野に連動、保存失敗の案内と結果画面の案内の重複と一覧の全再構築を解消。回帰テスト2件を追加 | `npm run check` 成功（106テスト）・実ブラウザ（390px）で解説の予定表示・手ごたえの選び直し・小分野の連動を確認 | 済み |
