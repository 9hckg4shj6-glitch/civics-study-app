/* ============================================================
   端末間同期の設定（公共・政治経済演習アプリ）
   ------------------------------------------------------------
   同期は「使う人が自分で置き場所を用意して、そこへ預ける」形にしてある。
   ここが空のままなら同期機能は画面に出るだけで、通信は一切起きない。

   endpoint … 同期コードでつなぐときに経由する中継サーバーのURL。
               worker/ にある Cloudflare Worker を deploy して得られる
               https://〜.workers.dev をそのまま入れる（末尾の / は不要）。
               中継サーバーに届くのは暗号化済みのデータだけで、
               同期コードを知らない限り中身は読めない。

   ※ このファイルは配布物（dist/）にそのまま入る。書き換えたら npm run deploy。
      端末側の「設定・データ → 端末間同期 → 詳細設定」で入力した値があれば、
      そちらが優先される（配布物を作り直さずに試せるようにするため）。
   ============================================================ */

window.SYNC_CONFIG = {
  endpoint: ""
};
